// This file previously contained TypeScript interface definitions.
// It has been converted to a .js file by removing all type-specific syntax.
// The structures are now implicitly defined by their usage in other JavaScript files.
// For properties like 'icon' that previously used 'React.ElementType', they are now implicitly 'any'.

// Original interfaces (for reference, no longer active in JS file)
/*
export interface Feature {
  id: string;
  name: string;
  icon: any; // Previously React.ElementType
  description: string;
  colorClass: string;
}

export interface FAQItem {
  question: string;
  answer: string;
}

export interface NavLink {
  name: string;
  path: string;
}

export interface SocialLink {
  name: string;
  icon: any; // Previously React.ElementType
  url: string;
}
*/